# Orchestration: Bender (Runtime Dev)

**Spawn time:** 2026-04-05T09:49:50Z  
**Mode:** background (2 separate spawns)  
**Duration spawn 1:** 1833s (30.5m)  
**Duration spawn 2:** 1391s (23m)  
**Model:** gpt-5.3-codex

## Mandate 1: AD-11 + AD-12 (Tools & Discovery)

### AD-11: ListDirectoryTool

**Location:** `src/core/BotNexus.CodingAgent/Tools/ListDirectoryTool.cs`

**Design:** Thin wrapper around `Directory.EnumerateFileSystemEntries()`.

**Parameters:**
- `path` (required)
- `depth` (optional, default 2)
- `showHidden` (optional, default false)

**Output:** Directory listing format (matches ReadTool directory mode, but no file content).

**Gitignore support:** Respect `.gitignore` filtering (same as GlobTool).

**Tests:** ListDirectoryToolTests in CodingAgent test suite.

### AD-12: ContextFileDiscovery

**Location:** `src/core/BotNexus.CodingAgent/Utils/ContextFileDiscovery.cs`

**Signature:**
```csharp
public static async Task<IReadOnlyList<PromptContextFile>> DiscoverAsync(
    string workingDirectory, CancellationToken ct);
```

**Discovery order:**
1. README.md (root only, if exists)
2. .github/copilot-instructions.md (if exists)
3. Files in docs/ (shallow, markdown only, capped)

**Token budget:** ~4000 tokens (~16KB) total. Truncate with `[truncated]` marker. Priority: copilot-instructions > README > docs/.

**Plug-in point:** `CodingAgent.CreateAsync()` line 46–53 where `SystemPromptContext` is built.

**Tests:** ContextFileDiscoveryTests covering truncation, priority order, binary file skipping.

## Mandate 2: AD-10 + AD-14 + AD-17 (CLI & Session)

### AD-10: --thinking CLI + runtime cycling

**Layer 1 — CLI:** `CommandParser.cs`, add `--thinking <level>` arg. Values: off, minimal, low, medium, high, xhigh → ThinkingLevel? → AgentOptions.GenerationSettings.Reasoning.

**Layer 2 — InteractiveLoop:** `InteractiveLoop.HandleCommandAsync()`, add `/thinking [level]` slash command. No arg = show current. With arg = set level.

**Layer 3 — Help:** Update `/help` output to include `/thinking`.

### AD-14: Session entry tracking

**MetadataEntry keys:**
- `"thinking_level_change"` with value `"{old} → {new}"`
- `"model_change"` with value `"{old} → {new}"`

**Emission points:**
- `/thinking` handler writes thinking_level_change
- `/model` handler writes model_change

**Implementation:** Call `SessionManager.WriteMetadataAsync()` from command handlers.

### AD-17: /thinking slash command

Covered by AD-10 (Layer 2). Update `/help` to document.

## Files (Mandate 1)

- `src/core/BotNexus.CodingAgent/Tools/ListDirectoryTool.cs` (new)
- `src/core/BotNexus.CodingAgent/Utils/ContextFileDiscovery.cs` (new)
- `src/core/BotNexus.CodingAgent/CodingAgent.cs` (wire discovery into CreateAsync)
- `tests/BotNexus.CodingAgent.Tests/Tools/ListDirectoryToolTests.cs` (new)
- `tests/BotNexus.CodingAgent.Tests/Utils/ContextFileDiscoveryTests.cs` (new)

## Files (Mandate 2)

- `src/core/BotNexus.CodingAgent/CommandParser.cs`
- `src/core/BotNexus.CodingAgent/InteractiveLoop.cs`
- `src/core/BotNexus.CodingAgent/SessionManager.cs` (write calls)
- `tests/BotNexus.CodingAgent.Tests/CommandParserTests.cs`
- `tests/BotNexus.CodingAgent.Tests/InteractiveLoopTests.cs`
- `tests/BotNexus.CodingAgent.Tests/SessionManagerTests.cs`

## Success Criteria (Mandate 1)

- [ ] ListDirectoryTool added and integrated into CodingAgent tools
- [ ] ContextFileDiscovery auto-discovery works end-to-end
- [ ] Token budget respected and truncation tested
- [ ] Gitignore filtering applied
- [ ] Build + test suite passes

## Success Criteria (Mandate 2)

- [ ] `--thinking` CLI arg parsed and threaded to agent options
- [ ] `/thinking` slash command works (show/set)
- [ ] Session entries written for thinking/model changes
- [ ] `/help` updated to document `/thinking`
- [ ] Build + test suite passes
